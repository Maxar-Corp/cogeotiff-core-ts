{
  "name": "@maxar/cogeotiff-core",
  "version": "1.1.0",
  "scripts": {
    "clean": "tsc -b --clean && rimraf 'build'",
    "build": "tsc -b --pretty",
    "build-watch": "tsc -b --pretty --watch",
    "test": "node --test",
    "prepare": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Maxar-Corp/cogeotiff-core-ts.git"
  },
  "type": "module",
  "engines": {
    "node": ">=16.0.0"
  },
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "author": "jerry.rumbolt@maxar.com",
  "license": "MIT",
  "dependencies": {
    "@chunkd/source": "11.4.0",
    "@chunkd/source-http": "11.4.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "lerna": "^9.0.7",
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
